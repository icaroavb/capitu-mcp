{
  "name": "@capitu/adt-client",
  "version": "0.0.1",
  "private": true,
  "description": "Thin wrapper around abap-adt-api with capitu-specific normalization, environment probing and compliance integration",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./probe": "./src/probe.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run"
  },
  "dependencies": {
    "@capitu/kb": "*",
    "abap-adt-api": "^8.4.0"
  }
}
